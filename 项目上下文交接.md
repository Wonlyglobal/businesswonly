# WONLY 获客系统 · 上下文交接（给未来的我 / 新会话 / Claude Code）

> 目的：换会话、换设备、或关机重开后，AI 能立刻接上进度。更新时间随手改。

## 一句话
WONLY 海外获客系统：纯静态站（GitHub Pages），线上 = `index.html` + `crawler/*.json`。
- 仓库：`chloe19980401/businesswonly`（分支 main）
- 域名：`business.foreverdoodle.com`
- 本地：`D:\ai项目\获客系统`
- 部署：改完文件 → 双击 `push-only.bat`（会先跑 `node crawler/gen-api.mjs` 生成 /api，再 git push）→ 等 1-2 分钟 → Ctrl+Shift+R 强刷。

## 现状（截至最近一次更新）
- `crawler/leads.json`：**2382 家线索 / 59 国**，每条含背调字段 + 契合度（5★ 512、4★ 1055）。**绝不编造**，每条带 sourceUrl。
- 前端 `index.html`：单文件 SPA。设计=Urbanist 字体 + 黑白极简 + 单点蓝 + 磨砂玻璃 + 深浅色切换（右下角）+ 门锁小插画。登录 wonly / wonly123，登录后直接进「客户线索库」。
- 导航精简为 8 个：常用(客户线索库·客户背调·市场调研·分析中心) + 更多(招标/项目·获客方式·任务与商机·数据源中心)。派发按 `data-page`（不是序号）。
- favicon：门锁小家伙（favicon.svg + png + ico）。
- 品牌名统一全大写 **WONLY**；侧栏用户 = WONLY · 海外事业部专用。
- 背调静态接口：`node crawler/gen-api.mjs` 生成 `api/company/<域名>.json`（约1518个）+ `api/index.json`，供"AI 开发信"系统对接。push 时自动重生成。
- 定时任务（云端）：每日挖新线索 + 每2小时处理背调申请，会读 Google 表单 + dd-requests.json。

## 进行中 / 待办
1. **业务反馈线索没人回**（info@ / 官网 WhatsApp 无效）。方向：找真实招标/RFQ + 关键人直邮/手机（不发 info@）。
   - 已选打法：**Apollo 免费版**（用户已注册登录）找采购负责人 + 直邮。
   - 卡点：Apollo 网页应用在浏览器自动化(后台标签)里加载不出来 → 改「用户在自己 Apollo 里搜、AI 教并回填」。
   - 试点从**沙特**起，第一家 **Akada Trading Company**（akada.com.sa, Riyadh）。等用户贴回 姓名/职位/邮箱/手机。
2. **wonly 官网**（另一个项目 `D:\ai项目\wonly官网`，域名 www.wonlyglobal.com，GitHub Pages + Actions 自动构建）：
   - 页脚/联系页邮箱源码**已是** `inquiry@wonlyglobal.com`，但**线上是旧构建**（还显示 `wonlyglobal@wonly.net`）→ 需重新部署（push main 触发 CI 重建）。
   - **还差**：`src/pages/legal/Legal.tsx` 仍有 3 处旧邮箱 `wonlyglobal@wonly.net` 未改；home 结构化数据里有个旧域名 `https://www.wonly.net/`（line 252）待确认是否改成 wonlyglobal.com。
3. （备选）市场部两页总结：重点工作 + AI 赋能提效（数据亮点段落已写好，见对话）。

## 提醒
- 云端沙箱无网络：git/部署要在本机跑。线索研究用 WebSearch/WebFetch 或 general-purpose Agent。
- 定时任务用 create_trigger（不是本地 cron）。
